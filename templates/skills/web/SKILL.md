# {{SKILL_NAME}}

{{SKILL_DESCRIPTION}}

## 适用场景
- 需要实现或改造 Web 前端功能（页面、组件、状态、路由、样式）
- 需要对现有代码进行小范围重构、修 bug、补测试
- 需要把需求拆解成可执行的工程步骤并落地到代码

## 输入（你需要提供）
- 目标仓库路径（默认当前工作区）
- 需求描述（期望行为 / 边界条件 / 交互细节）
- 相关文件路径或关键入口（若已知）
- 约束条件（框架/语言/样式方案/兼容性/上线节奏）

## 输出（我会交付）
- 可运行的代码改动（必要时附带最小示例/模板）
- 关键文件与改动点说明
- 可执行的验证方式（本地运行/构建/测试步骤）

## 工作方式
- 先定位入口：路由/页面/组件/接口/状态管理
- 再最小改动落地：优先保证正确性与可维护性
- 最后做校验：能跑、能构建、基础质量检查通过

## 约束与偏好
- 默认使用 TypeScript（若项目已使用）
- 组件优先函数式 + 清晰的 props/事件边界
- 不引入不必要依赖；若要加依赖会说明原因
- 只写有意义的注释（解释意图/权衡，不叙述代码）

## 示例指令（给 AI 的）
- “在 `@src/pages/Home.vue` 加一个搜索框，输入时做防抖请求，结果高亮匹配文本；要支持回车提交。”
- “把这段重复逻辑抽成一个 composable，并补上单测（Vitest）。”

---
模板版本：{{YEAR}}
